#include <stdio.h>

int main() {
    int num1 = 5;
    int num2 = 3;
    if (num1 > num2) {
        printf("Number one is greater than number two\n");
    } else {
        printf("Number one is less than or equal to number two\n");
    }
    return 0;
}
